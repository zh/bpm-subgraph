{
  "name": "bpm-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ zh/bpm-subgraph",
    "create-local": "graph create --node http://localhost:8020/ zh/bpm-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ zh/bpm-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zh/bpm-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.0",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
